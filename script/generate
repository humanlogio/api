#!/usr/bin/env bash

set -e

# eval "$(pkgx --shellcode)"
root=$(git rev-parse --show-toplevel)

function main() {
    pushd ${root}
    generate_protobuf
}

function generate_protobuf() {
    export PATH=${PATH}:${root}/node_modules/.bin
    pkgx buf format -w
    pkgx buf generate
}

main
